description: "Catalog Interactive Render Test"
requires:
  archetect: "3.0.0"

catalog:
  services:
    description: "Backend Services"
    catalog:
      rest:
        description: "REST Service"
        source: "../lua_catalog_render_tests/child-archetype"
