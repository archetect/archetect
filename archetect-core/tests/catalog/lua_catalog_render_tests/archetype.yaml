description: "Catalog Render Test"
requires:
  archetect: "3.0.0"

catalog:
  services:
    description: "Backend Services"
    catalog:
      grpc:
        description: "gRPC Service"
        source: "child-archetype"
        answers:
          service_name: "grpc-service"
      rest:
        description: "REST Service"
        source: "child-archetype"
